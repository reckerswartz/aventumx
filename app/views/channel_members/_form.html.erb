<%= form_with(model: channel_member) do |form| %>
  <% if channel_member.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(channel_member.errors.count, "error") %> prohibited this channel_member from being saved:</h2>

      <ul>
        <% channel_member.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.text_field :status %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
